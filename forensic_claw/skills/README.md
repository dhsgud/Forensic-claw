# Built-in Skills

This directory contains built-in forensic-claw skills that can be loaded into agent context.

- Each skill lives in its own folder.
- A skill is enabled by the presence of `SKILL.md`.
- Optional helper resources can live under `scripts/`, `references/`, or `assets/`.
